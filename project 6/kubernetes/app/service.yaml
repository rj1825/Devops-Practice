apiVersion: v1
kind: Service
metadata:
  name: sample-web-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: sample-web-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
